import React from 'react'
import Button from '../Button'
import HighLightText from './HighLightText'
import {FaArrowRight} from "react-icons"

const CodeBlocks = ({postion,heading,subheading,ctable1 }) ={
    return(
        <div>

        </div>
    )
}